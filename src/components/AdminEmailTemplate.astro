---
interface Props {
  firstName: string;
  message: string;
}

const { firstName, message } = Astro.props;

---

<div>
    <h1>New Message From { firstName }</h1>
    <p>They sent... <span>{message}</span></p>
</div>